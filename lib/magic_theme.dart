/// A lightweight, reusable style-bundle for Flutter — define a [MagicTheme]
/// once and apply its pieces (text styles, colors, spacing, etc.) wherever
/// you need them.
library;

export 'src/magic_theme.dart';
